require "spec_helper"

RSpec.describe Ruboty::HttpSample do
  it "has a version number" do
    expect(Ruboty::HttpSample::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
